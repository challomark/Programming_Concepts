#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Using right shift operator
 *
 * Return: Always (0)
 */
int main(void)
{
	int x = 49;
	int result = x >> 3;

	printf("%d\n", result);
	return (0);
}

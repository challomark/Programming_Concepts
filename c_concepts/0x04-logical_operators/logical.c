#include <stdio.h>

/**
 * main - entry point
 *
 * Description: Using logical operators
 * Return: Always 0 (success)
 */

int main(void)
{
	int results = 5 >= 4 || 3 > 1;

	printf("%d \n", results);
	return (0);
}
